# NPM Configuration
registry=https://registry.npmjs.org/
access=public

# Ensure package.json is not ignored
package-lock=true

# Don't save exact versions for optional dependencies
save-exact=false

# Use npm ci for faster installs in CI
prefer-offline=true 